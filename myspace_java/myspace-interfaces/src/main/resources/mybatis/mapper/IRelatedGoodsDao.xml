<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.myspace.infrastructure.dao.IRelatedGoodsDao">

    <!--新增所有列-->
    <insert id="insert" keyProperty="manuscriptId">
        insert into related_goods(manuscript_id, goods_id, creator, create_time, update_time, delete_mark)
        values (#{manuscriptId}, #{goodsId}, #{creator}, sysdate(), sysdate(), 0)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update related_goods
        <set>
                update_time = sysdate(),
            <if test="deleteMark != null">
                delete_mark = #{deleteMark},
            </if>
        </set>
        where manuscript_id = #{manuscriptId}
        and goods_id = #{goodsId}
    </update>

</mapper>