<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.myspace.infrastructure.dao.IUserRoleDao">

    <!--新增所有列-->
    <insert id="insert" keyProperty="userId" >
        insert into user_role(user_id, role_id, create_time, update_time, delete_mark)
        values (#{userId}, ${roleId}, sysdate(), sysdate(), 0)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user_role
        <set>
            <if test="role_id != null">
                role_id = ${roleId},
            </if>
                update_time = sysdate(),
            <if test="deleteMark != null">
                delete_mark = #{deleteMark},
            </if>
        </set>
        where user_id = #{userId}
    </update>

</mapper>