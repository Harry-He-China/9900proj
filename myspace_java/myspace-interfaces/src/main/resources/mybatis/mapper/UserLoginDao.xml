<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.myspace.infrastructure.dao.IUserLoginDao">

    <resultMap type="cn.myspace.infrastructure.po.UserLogin" id="UserLoginMap">
        <result property="infoId" column="info_id"/>
        <result property="userId" column="user_id"/>
        <result property="ipaddr" column="ipaddr"/>
        <result property="loginLocation" column="login_location"/>
        <result property="browser" column="browser"/>
        <result property="os" column="os"/>
        <result property="status" column="status"/>
        <result property="msg" column="msg"/>
        <result property="loginTime" column="login_time"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserLoginMap">
        select info_id,
               user_id,
               ipaddr,
               login_location,
               browser,
               os,
               status,
               msg,
               login_time
        from user_login
        where info_id = #{infoId}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="infoId">
        insert into user_login(info_id, user_id, ipaddr, login_location, browser, os, status, msg, login_time)
        values (#{infoId}, #{userId}, #{ipaddr}, #{loginLocation}, #{browser}, #{os}, #{status}, #{msg}, sysdate())
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user_login
        <set>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="msg != null and msg != ''">
                msg = #{msg},
            </if>
        </set>
        where info_id = #{infoId}
    </update>

</mapper>