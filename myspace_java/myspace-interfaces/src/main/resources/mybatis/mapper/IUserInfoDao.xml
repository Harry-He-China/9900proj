<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.myspace.infrastructure.dao.IUserInfoDao">

    <resultMap type="cn.myspace.infrastructure.po.UserInfo" id="UserInfoMap">
        <result property="userId" column="user_id"/>
        <result property="name" column="name"/>
        <result property="age" column="age"/>
        <result property="country" column="country"/>
        <result property="address" column="address"/>
        <result property="recipes" column="recipes"/>
        <result property="intro" column="intro"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserInfoMap">
        select
        user_id, name, age, country, address, recipes, intro, create_time, update_time, delete_mark
        from user_info
        where user_id = #{userId}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="userId" >
        insert into user_info(user_id, name, age, country, address, recipes, intro, create_time, update_time,
        delete_mark)
        values (#{userId}, #{name}, #{age}, #{country}, #{address}, #{recipes}, #{intro}, sysdate(), sysdate(), 0)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update user_info
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="age != null">
                age = #{age},
            </if>
            <if test="country != null and country != ''">
                country = #{country},
            </if>
            <if test="address != null">
                address = #{address},
            </if>
            <if test="recipes != null and recipes != ''">
                recipes = #{recipes},
            </if>
            <if test="intro != null">
                intro = #{intro},
            </if>
                update_time = sysdate(),
            <if test="deleteMark != null">
                delete_mark = #{deleteMark},
            </if>
        </set>
        where user_id = #{userId}
    </update>

</mapper>